name: Build Reel 03 Protein Infographic (legacy manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Legacy Reel 03 workflow disabled from automatic push builds. Use assemble-protein-reel.yml."
